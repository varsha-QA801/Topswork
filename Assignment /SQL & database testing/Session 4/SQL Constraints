1.
Create a table called Playlist with columns: song_id (integer), song_name (varchar), artist (varchar), and set song_id as the PRIMARY KEY.
Answer:

CREATE TABLE Playlist (
    song_id INT PRIMARY KEY,
    song_name VARCHAR(100),
    artist VARCHAR(100)
);

2.
Create a table called UserProfile with columns: user_id (integer, PRIMARY KEY), username (varchar, UNIQUE), email (varchar, NOT NULL), and city (varchar, DEFAULT 'Ahmedabad').
Answer:

CREATE TABLE UserProfile (
    user_id INT PRIMARY KEY,
    username VARCHAR(50) UNIQUE,
    email VARCHAR(100) NOT NULL,
    city VARCHAR(50) DEFAULT 'Ahmedabad'
);

3.
Create a table called Follows to represent Instagram-style followers with columns: follower_id (integer), followed_id (integer), and set both as FOREIGN KEYs referencing user_id in UserProfile.
Answer:

CREATE TABLE Follows (
    follower_id INT,
    followed_id INT,
    FOREIGN KEY (follower_id) REFERENCES UserProfile(user_id),
    FOREIGN KEY (followed_id) REFERENCES UserProfile(user_id)
);

4.
Try to insert a new UserProfile row with a NULL value for email and observe the error message generated by the NOT NULL constraint.<br><br><em><strong>Hint:</strong> Note the exact error text returned by your database system.</em>
Answer:

INSERT INTO UserProfile (user_id, username, email, city)
VALUES (1, 'vishnu', NULL, 'Ahmedabad');

Error:
ERROR 1048 (23000): Column 'email' cannot be null

5.
Add a CHECK constraint to the Playlist table so that song_name must be at least 3 characters long, then attempt to insert a song with a 2-character name and record the error message.
Answer:

1. Add CHECK constraint
ALTER TABLE Playlist
ADD CONSTRAINT chk_song_name
CHECK (CHAR_LENGTH(song_name) >= 3);

2.insert a 2-character song name
INSERT INTO Playlist (song_id, song_name, artist)
VALUES (1, 'AB', 'Arijit Singh');

Error:
ERROR 3819 (HY000): Check constraint 'chk_song_name' is violated.

